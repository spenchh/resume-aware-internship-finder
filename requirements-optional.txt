# Optional dependencies — the tool degrades gracefully without these.
#   anthropic   -> optional Claude fallback for AI match scoring. OpenRouter
#                  uses the core requests dependency.
#   streamlit   -> interactive dashboard (Phase 3, Section 9).
anthropic>=0.40
streamlit>=1.40
