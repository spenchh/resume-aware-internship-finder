# Core dependencies — required for the base tool.
streamlit>=1.40       # web UI (the upload-and-run app: streamlit_app.py)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.1
pdfplumber>=0.11      # robust PDF text extraction (handles multi-column resumes)
python-docx>=1.1      # DOCX resume parsing
PyYAML>=6.0           # config files
python-dateutil>=2.9  # tolerant date parsing for schema.org / curated lists
