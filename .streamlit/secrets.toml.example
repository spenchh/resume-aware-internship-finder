# Copy this file to ".streamlit/secrets.toml" for LOCAL runs, or paste the same
# keys into Streamlit Community Cloud under  Settings → Secrets  when deployed.
# Every key is OPTIONAL — the app works without any of them.

# ANTHROPIC_API_KEY = "sk-ant-..."   # Claude AI match scoring (matching.use_llm)
# SERPAPI_API_KEY   = "..."          # Google Jobs source
# GITHUB_TOKEN      = "..."          # higher rate limit for curated GitHub lists
